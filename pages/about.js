import React from "react";
import styles from "../styles/About.module.css";

const About = () => {
	return (
		<div>
			<h2 className={styles.higlight}>About Page</h2>
			<button className="btn btn-primary">Primary</button>
		</div>
	);
};

export default About;
